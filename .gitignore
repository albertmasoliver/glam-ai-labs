# Local envs / caches
.venv/
__pycache__/
.pytest_cache/
.coverage
.coverage.json

# Training output + the saved model exercise 10 writes / exercise 11 loads
out/
finetuned-demo/

# Third-party source materials — never commit (copyright)
*chapter*.pdf
Ch*-Ex*.png
